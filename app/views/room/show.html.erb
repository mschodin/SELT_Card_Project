<!-- update id tag is used for the ActionCable refresh page response DONT DELETE -->
<div id = 'update'><div>
<%= react_component("GameRoom", {
    handId: @room.game_hands.find(@player1[:id]).id,
    playerHand: @room.game_hands.find(@player1[:id]).display_hand_with_id,
    piles: @room.get_piles_and_cards,
    piles_to_deck: @piles_to_deck,
    create_deck_urls: room_pile_deck_url(session[:room_id], "__pile_id__"),
    roomId: session[:room_id],
    players: @player_info,
    draw_multiple: room_draw_multiple_url(params[:id]),
    player: session[:player],
    room_passcode: @room.code
} ) %>